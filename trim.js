const value = ' Hello Boys ';
console.log(value.trimStart());
console.log(value.trimEnd());
console.log(value.trim());
//trim method removes all the whitespace start and end site
//trimStart method remove only start whitespaces//trimEnd is oppsite
